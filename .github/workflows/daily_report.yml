name: daily_report
on: [push]
jobs:
  notify_line:
    runs-on: ubuntu-latest
    steps:
    - name: push
      env: 
          TITLE: ${{ github.event.commits.massage }}
          
      run: echo "complete push"
      
    - name: send LINE Notify
      env: 
        LINE_TOKEN: ${{ secrets.LINE_TOKEN }}
        URL: https://notify-api.line.me/api/notify
      run: echo "test"
